pipeline {
    agent any

    stages {

        stage('Clone Repository') {
            steps {
                script {
                    // Define the repository URL
                    sh "ls"
                }
            }
        }

        stage('Change Directory and Run Python Script') {
            steps {
                script {
                        sh "ls"
                    }
                }
            }
        }
    }
